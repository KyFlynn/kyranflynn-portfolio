{
  "name": "kyran-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:css": "npx tailwindcss -i ./styles/input.css -o ./styles/output.css --watch",
    "dev:site": "npx @11ty/eleventy --serve --watch",
    "build:css": "npx tailwindcss -i ./styles/input.css -o ./styles/output.css --minify",
    "build:site": "npx @11ty/eleventy",
    "build": "npm run build:css && npm run build:site"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.13"
  }
}
